import { state as authState, mutations as authMutations } from './auth'
export const state = () => ({})
export const mutations = {}
export const actions = {}
export const getters = {}

export const modules = {
  auth: {
    state: authState,
    mutations: authMutations
  }
}
